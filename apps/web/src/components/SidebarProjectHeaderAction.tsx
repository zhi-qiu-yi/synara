// FILE: SidebarProjectHeaderAction.tsx
// Deprecated: project header actions now use SidebarIconButton inside
// SidebarSectionToolbar (same pattern as the Chats header). Kept only so
// imports do not break during refactors; safe to delete once unused.

export {};
